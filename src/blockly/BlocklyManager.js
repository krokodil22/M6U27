import * as Blockly from 'blockly';import {registerBlocks} from './customBlocks.js';import {BlocklyRuntime} from './BlocklyRuntime.js';
registerBlocks();
export class BlocklyManager{
 constructor(el,engine){this.el=el;this.engine=engine;this.workspace=null;this.runtime=new BlocklyRuntime(this,engine)}
 load(level){if(this.workspace)this.workspace.dispose();this.el.innerHTML='';this.workspace=Blockly.inject(this.el,{toolbox:this.toolbox(level),trashcan:true,zoom:{controls:true,wheel:true},grid:{spacing:22,length:2,colour:'#dfe5fb',snap:true},theme:Blockly.Theme.defineTheme('soft',{base:Blockly.Themes.Classic,componentStyles:{workspaceBackgroundColour:'#ffffff',toolboxBackgroundColour:'#efefef',flyoutBackgroundColour:'#efefef',flyoutOpacity:1,scrollbarColour:'#bcbcbc'}})});for(const name of Object.keys(this.engine.config.vars||{}))this.workspace.createVariable(name);this.workspace.addChangeListener(()=>this.engine.syncVariables());}
 toolbox(l){const b=t=>({kind:'block',type:t});const contents=[];if(l===1){contents.push({kind:'label',text:'На первом уровне\nуправляй роботом стрелками'});return {kind:'flyoutToolbox',contents}}if(l>=3)contents.push({kind:'button',text:'＋ Создать переменную',callbackKey:'CREATE_VAR'},b('event_start'));contents.push(b('event_bolt'),b('event_fuel'));if(l===4)contents.push(b('event_station'));if(l===5)contents.push(b('event_fork'));if(l>=3)contents.push(b('set_var'));contents.push(b('change_var'),b('math_number'));if(l===4)contents.push(b('station_cost'));if(l===5)contents.push(b('robot_if'),b('logic_compare'),b('variables_get'),b('short_path'),b('long_path'));setTimeout(()=>this.workspace?.registerButtonCallback('CREATE_VAR',()=>{const name=prompt('Имя переменной («болтики» или «топливо»):')?.trim().toLowerCase();if(['болтики','топливо'].includes(name)){this.engine.createVariable(name);this.workspace.createVariable(name)}else if(name)this.engine.ui.toast('Используй имя «болтики» или «топливо».','error')}));return {kind:'flyoutToolbox',contents}}
 highlight(id){this.workspace?.highlightBlock(id)}clear(){this.workspace?.clear()}
}
